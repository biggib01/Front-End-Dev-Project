    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-md">
            <a class="navbar-brand" href="/">Pizza de</a>
        </div>
        <span class="navbar-text">
            <button class="btn btn-outline-success my-2 my-sm-0" id="myBtn" type="submit">Search</button>
        </span>
    </nav>

    <!-- The Modal -->
    <div id="myModal" class="modal">

        <!-- Modal content -->
        <div class="modal-content">
        <div class="modal-header">
            <span class="close">&times;</span>
        </div>
        <div class="modal-body">
            <div class="row justify-content-center text-center">
                <div class="col-md-8 col-lg-6">
                    <h2 id="table"></h2>
                </div>
            </div>
            <%- include('partials/orderList') %> 
        </div>
        <div class="modal-footer">
        </div>
        </div>

    </div>

    <%- include('partials/main'); %> 

    <div class="testDiv">
        <div class="nav">
            <ul class="nav nav-tabs">
                    <li class="nav-item">
                      <a class="nav-link" href="/">Pizza</a>
                    </li>
                    <li class="nav-item">
                       <a class="nav-link" href="/pasta">Pasta</a>
                    </li>       
                    <li class="nav-item">
                      <a class="nav-link active" aria-current="page" href="/drinks">Drinks</a>
                    </li>
                </ul>
        </div>
        <%- include('partials/menuOther') %> 
    </div>

    <%- include('partials/footer') %> 

    <script>
        // Get the modal
        var modal = document.getElementById("myModal");
        
        // Get the button that opens the modal
        var btn = document.getElementById("myBtn");

        var bt = document.getElementById("myBt");
        
        // Get the <span> element that closes the modal
        var span = document.getElementsByClassName("close")[0];
        
        // When the user clicks the button, open the modal 
        btn.onclick = function() {
            modal.style.display = "block";
            
        }

        bt.onclick = function() {
            var id = document.getElementById("idTable");
            var strUser = id.options[id.selectedIndex].text;
            var number = document.getElementById("numberTable");
            var strUser1 = number.options[number.selectedIndex].text;

            var keep = strUser+strUser1;

            modal.style.display = "block";
            
        }
        
        // When the user clicks on <span> (x), close the modal
        span.onclick = function() {
            modal.style.display = "none";
        }
        
        // When the user clicks anywhere outside of the modal, close it
        window.onclick = function(event) {
            if (event.target == modal) {
                modal.style.display = "none";
            }
        }
    </script>